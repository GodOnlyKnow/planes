@model Planes.Models.Goods
@{
    ViewBag.Title = "添加";
    ViewBag.SiderBar = 1;
}

<hr />
@using (Html.BeginForm("CreateB", "Good", FormMethod.Post, new { @class = "form-horizontal", role = "form", enctype = "multipart/form-data" }))
{
    <input type="hidden" name="airportId" value="@ViewBag.airportId" />
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            @Html.ValidationSummary("", new { @class = "text-danger" })
        </div>
    </div>
    <div class="form-group">
        <label class="control-label col-md-2">航线</label>
        <div class="col-md-3">
            @Html.TextBoxFor(m => m.col1, new { @class = "form-control" })
        </div>
        <label class="control-label col-md-1">——</label>
        <div class="col-md-3">
            @Html.TextBoxFor(m => m.col2, new { @class = "form-control" })
        </div>
    </div>
}

